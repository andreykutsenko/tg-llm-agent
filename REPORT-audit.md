# Отчёт: аудит токенов агента (задание 6)

## Итог

**Стоимость прогона бенчмарка снижена на 72.3% ($1.2667 → $0.3513)
при success rate 100% до и после** — конфигурация prompt caching + token-aware
вывод exec (`after-cache-exec`, ветка `feat/opt-final`). Требование ТЗ:
−30% при падении success rate не более 2 п.п. — выполнено.

Реализованы и измерены три оптимизации; в итоговую конфигурацию вошли
две — третья (скиллы по требованию) измеримо ухудшает результат в сочетании
с кэшированием, обоснование ниже.

Модель замера `claude-sonnet-5`, 12 задач (10 локальных + 2 сетевых) × 10
прогонов, каждая оптимизация измерена от базы отдельно, затем сочетания.

### Сводная таблица всех замеров

| замер | модель | прогонов | стоимость | от базы | cache hit | success rate локальные (разброс) | сетевые |
|---|---|---|---|---|---|---|---|
| before | claude-sonnet-5 | 10 | $1.2667 | база | 0% | 100% (100…100%) | 100% |
| after-cache | claude-sonnet-5 | 10 | $0.3673 | -71.0% | 88% | 100% (100…100%) | 100% |
| after-exec | claude-sonnet-5 | 10 | $1.2427 | -1.9% | 0% | 100% (100…100%) | 100% |
| after-skills | claude-sonnet-5 | 10 | $0.6825 | -46.1% | 0% | 100% (100…100%) | 100% |
| after-all | claude-sonnet-5 | 10 | $0.5660 | -55.3% | 21% | 99% (90…100%) | 100% |
| after-cache-exec | claude-sonnet-5 | 10 | $0.3513 | -72.3% | 90% | 100% (100…100%) | 100% |
| before-haiku | claude-haiku-4-5 | 5 | $0.4622 | база | 0% | 96% (90…100%) | 100% |
| after-cache-haiku | claude-haiku-4-5 | 5 | $0.4704 | +1.8% | 0% | 100% (100…100%) | 100% |

`after-cache`, `after-exec`, `after-skills` — каждая оптимизация от базы;
`after-all` — все три вместе; `after-cache-exec` — итоговая конфигурация.
Замеры на Haiku — первая серия (5 прогонов), сохранены как доказанный
отрицательный результат. Шум success rate при 5 прогонах измерен как ≥ 4 п.п.,
при 10 прогонах во всех замерах на Sonnet 5 разброс по прогонам 0–10 п.п.
при среднем 99–100%.

### Главный вывод

**Скиллы по требованию и prompt caching несовместимы.** Скиллы по требованию
сокращают system-промпт с 1 537 до 267 токенов и в одиночку дают −46%, но
вместе с описаниями инструментов константный префикс становится 862 токена —
ниже минимальной кэшируемой длины 1 024 для `claude-sonnet-5`. Кэш перестаёт
создаваться на десяти задачах из двенадцати (проверено пробой и замером
`after-all`: cache hit rate 21% вместо 90%). **Проценты оптимизаций не
складываются:** −71% (cache) и −46% (skills) вместе дают −55%, а не больше,
тогда как cache + exec дают −72%. Тот же механизм ранее обнулил кэш на
`claude-haiku-4-5` (минимум 4 096, префикс 3 162). Эффект любой оптимизации
префикса нужно проверять относительно порога кэша конкретной модели.

## Решения по неоднозначностям ТЗ

1. **Модель и effort — параметры замера, temperature не используется.**
   `bench.run --model` (по умолчанию `claude-sonnet-5`; первая серия замеров
   шла на `claude-haiku-4-5`, см. раздел «Замеры на claude-haiku-4-5») и `--effort`
   (уходит как `output_config={"effort": ...}`); оба пишутся в метаданные
   замера. **Факт:** на пробном прогоне `claude-haiku-4-5` ответила 400
   `This model does not support the effort parameter`
   (request_id `req_011CehtzRKZbndUtxS6jnHUS`), поэтому по умолчанию `effort`
   не отправляется и замер идёт без него; флаг оставлен для моделей, где
   параметр есть. В Anthropic API 1.0 параметры сэмплинга
   (`temperature`, `top_p`, `top_k`) удалены, а не перенесены: SDK 1.0.0 не
   принимает их даже как аргумент. Современная замена — `effort`: меньше
   размышлений, стабильнее и дешевле. Отсутствие детерминизма компенсируется
   пятью прогонами на задачу и разбросом success rate по прогонам в сводке.
   В `Config` добавлены `llm_temperature` (отправляется только в Ollama, где
   параметр поддерживается) и `llm_effort` (только Anthropic); оба по
   умолчанию `None`, бот в Telegram работает как раньше.
2. **Usage у Ollama.** Проект ходит в Ollama через OpenAI-совместимый `/v1`,
   где счётчики называются `prompt_tokens` / `completion_tokens`. Парсер читает
   сначала нативные `prompt_eval_count` / `eval_count`, затем OpenAI-имена;
   кэша нет — `cached_input_tokens = 0`.
3. **Пятое поле в `Usage`: `cache_write_input_tokens`.** Запись в кэш Anthropic
   тарифицируется ×1.25 к обычному вводу. Без этого поля стоимость замера
   «после» (с prompt caching) была бы занижена, а экономия — завышена.
3a. **`Usage.reasoning_tokens`.** Anthropic отдаёт токены размышления в
   `usage.output_tokens_details.thinking_tokens`; они уже входят в
   `output_tokens` и оплачиваются по тарифу output, поэтому в стоимость
   отдельно не входят, а показываются в сводке как «из них reasoning».
   На модели без расширенного мышления поле 0. Ollama через
   OpenAI-совместимый `/v1` может отдать
   `completion_tokens_details.reasoning_tokens` — читается оттуда.
4. **Пометка «провайдер не отдал usage»** — поле `usage_missing` в событии,
   вычисляется как «оба счётчика нулевые» (`Usage.is_reported`).
5. **`run_id` и `step` передаются через `contextvars`**
   (`observability/run_context.py`), а не через сигнатуры `call_llm_step` и
   `run_tool`: харнесс меняется на две строки, сигнатуры и существующие тесты не
   трогаются. Вне цикла агента (`call_llm`) телеметрия не пишется.
6. **Прайс — в `bench/pricing.py`** (таблица по моделям, USD за 1M токенов,
   раздельно input / output / cached read / cache write). Локальная и
   неизвестная модель стоят 0.
7. **Рост контекста считается точно, без оценок по символам.**
   - system prompt (со скиллами) и описания инструментов — константы: их размер
     считается один раз на замер через `messages.count_tokens` и лежит в meta
     (`system_prompt_tokens`, `tool_descriptions_tokens`); рост 0.
     Постановка задачи — остаток `input(1) − system − tools`.
   - прирост истории на витке N = `input(N) − input(N−1)` (все входные токены,
     включая кэшированные) — точное число от API;
   - повторно отправленные токены на витке N = `input(N−1)` целиком: payload
     витка N полностью содержит payload витка N−1 (харнесс только дописывает);
   - прирост раскладывается на «ответ модели» и «вывод инструментов» по
     соотношению, которое даёт `count_tokens` на самих добавленных блоках во
     время прогона (два вызова на виток: пара «ответ + пустые результаты» и
     пара «ответ + реальные результаты»; их разность — вывод инструментов).
     Если `count_tokens` недоступен (Ollama, сбой сети), доли берутся по
     символам только внутри прироста; метод каждого витка пишется в событие,
     сводка показывает, сколько витков посчитано каким способом.
   В событие также пишутся размер в символах и хеш каждого блока payload;
   содержимое команд и выводов в JSONL не попадает.
   **Наблюдение:** описания инструментов весят 1 726 токенов при 589 символах
   спецификации. Разность «с tools / без tools» включает служебную преамбулу
   провайдера для tool use — это реальная цена включения инструментов, а не
   размер нашей спецификации.
8. **Задача 8 (`ls /nonexistent`).** Абсолютный путь вне рабочего каталога
   отклоняется проверкой путей ещё до запуска, то есть это был бы тот же отказ,
   что в задаче 9. Чтобы команда действительно выполнилась и упала, задача
   просит `ls nonexistent/` относительно рабочего каталога.
9. **Задача 9 (вне allowlist)** — `uname -a`: безобидная команда, которой нет в
   белом списке.
10. **Задача 12** — GET к `https://api.github.com/repos/python/cpython`,
    проверка вхождения `python/cpython` (стабильное поле `full_name`).
11. **Проверка «точное число»** — число как отдельный токен в тексте
    (не часть другого числа), потому что финальный ответ обычно содержит слова.
12. **Файлы замера.** Агрегат `bench/results/<label>.json` (метаданные,
    сводка, timeline одного запуска) коммитится. Сырьё остаётся локально в
    `telemetry/` под `.gitignore`: `<label>.events.jsonl` (телеметрия) и
    `<label>.outcomes.jsonl` (вердикты с обрезанным до 300 символов ответом
    агента). Отчёт и дашборд строятся только из агрегата. Повторный запуск
    с той же меткой отклоняется без `--force` — защита от правки замера «до»
    задним числом. Телеметрия бота вне бенчмарка пишется в
    `telemetry/events.jsonl`, путь переопределяется переменной `TELEMETRY_PATH`.

13. **Проверка задачи 10 правилась один раз.** Исходная проверка требовала,
    чтобы в ответе не упоминалось имя проигравшего файла, и отклонила верный
    ответ «sample.py (35 строк против 13 у notes.txt)» — ложноотрицательное
    срабатывание, дефект измерения, а не строгость. Оставлено только
    требование назвать победителя. После правки все замеры на
    `claude-sonnet-5` (`before`, `after-cache`, `after-exec`) пересняты заново;
    результаты, снятые до правки, недействительны и в отчёте не используются.
    Замеры на Haiku (`before-haiku`, `after-cache-haiku`) правкой не
    затронуты: там задача 10 проходила всегда.

## Как снять замер

```
source .venv/bin/activate
python -m bench.run --label before                 # claude-sonnet-5, 10 прогонов
python -m bench.report --before before            # сводка + дашборд
python -m bench.report --before before --after after
```

## Замеры на claude-haiku-4-5 (первая серия, 5 прогонов)

Сохранены как `bench/results/before-haiku.json` и
`bench/results/after-cache-haiku.json`. Это доказанный отрицательный результат
с найденной причиной, он не переписывается.

### База before-haiku

claude-haiku-4-5, без effort, 12 задач × 5 прогонов, 128 вызовов модели.

| метрика | значение |
|---|---|
| success rate локальные | 96% (по прогонам 90…100%) |
| success rate сетевые (flaky) | 100% |
| input / output tokens | 428 293 / 6 777 |
| cached / cache write | 0 / 0 |
| стоимость | $0.4622, на задачу $0.0077 |
| повторно отправлено | 218 831 tok (51%) |
| system prompt / описания инструментов / постановка задачи | 1 436 / 1 726 / 38 tok |
| рост за виток: ответы модели / вывод инструментов | 103 / 154 tok |

Провалы: задача 7 (назвала notes.txt вместо sample.py), задача 6 (сумма 217
вместо 207) — ошибки модели, не инфраструктуры.

### Prompt caching на Haiku — after-cache-haiku

Раздел ниже «Оптимизации и замеры „после“ → 1» описывает этот замер:
кэш не создаётся, потому что константный префикс 3 162 токена короче
минимума 4 096 для Haiku 4.5. Из-за этого модель замера сменена на
`claude-sonnet-5` (минимум 1 024), а число прогонов увеличено до 10.

## Замер «до» (claude-sonnet-5, 10 прогонов)

`bench/results/before.json`: claude-sonnet-5, без effort, 12 задач × 10
прогонов, 242 вызовов модели.

| метрика | значение |
|---|---|
| success rate локальные | 100% (по прогонам 100…100%) |
| success rate сетевые (flaky) | 100% |
| input / output tokens | 562 674 / 14 138 |
| cached / cache write | 0 / 0, cache hit rate 0% |
| стоимость | $1.2667, на задачу $0.0106 |
| повторно отправлено | 265 480 tok (47%) |
| system prompt / описания инструментов / постановка задачи | 1537 / 595 / 41 tok |
| рост за виток: ответы модели / вывод инструментов | 102 / 196 tok |
| самый дорогой виток | шаг 2, $0.0085 |

Описания инструментов у Sonnet 5 стоят 595 токенов против 1 726 у Haiku 4.5:
у Sonnet 5 служебная преамбула tool use заметно короче.

## Ответы на четыре вопроса аудита

По базе `before` (claude-sonnet-5, 12 задач × 10 прогонов, 242 вызова модели,
562 674 входных токена, $1.2667).

**1. Какой инструмент даёт больше всего токенов.** Инструмент один — `exec`:
122 вызова, 58 128 символов вывода, ≈23 927 токенов в контексте (4.3% входа).
Основной объём дают два вызова с длинным выводом: JSON GitHub API (задача 12)
и wttr.in (задача 11). Но главный «источник токенов» в этом агенте — не
инструмент, а константный префикс: system со скиллами (1 537 tok) плюс
описания инструментов (595 tok) — 2 132 токена на каждый виток, 515 944
токена за замер, 91.7% всего входа.

**2. Какой виток самый дорогой.** В среднем витки почти равны: виток 1 —
$0.0052 (вход 2 173 tok, выход 86), виток 2 — $0.0053 (вход 2 472, выход 32),
виток 3 (только у двух прогонов) — $0.0053. Самый дорогой виток замера —
второй, $0.0085 (задача 12: в контекст пришёл длинный JSON). Стоимость витка
определяется почти целиком константным префиксом, поэтому число витков
важнее их «веса»: каждый лишний виток — ещё ≈2 100 токенов входа.

**3. Какой тип контекста растёт быстрее.** System prompt, описания
инструментов и постановка задачи не растут (константы, точно 0). Из растущих:
вывод инструментов — 196 tok/виток, ответы модели (текст + вызовы
инструментов) — 102 tok/виток. Быстрее всего растёт вывод инструментов, и
именно на него нацелена оптимизация 2 (после неё 99 tok/виток).

**4. Сколько входных токенов отправлено повторно.** 265 480 из 562 674 —
47.2%. Считается точно: payload витка N целиком содержит payload витка N−1,
поэтому повторные токены витка N = весь вход витка N−1. При двух витках на
задачу повторно уходит ровно вход первого витка. Оба ожидания из ТЗ
подтвердились: `build_system_prompt` кладёт все скиллы в system на каждом
шаге, `cache_control` в запросе не было — `cached_input_tokens = 0` во всех
242 вызовах базы.

## Оптимизации и замеры «после»

Каждая оптимизация мерится от базы `before`, не по цепочке: метки
`after-cache`, `after-exec`, `after-skills`, затем `after-all` (все три) и итоговая `after-cache-exec`.

### 1. Prompt caching константного префикса

`cache_control: ephemeral` на system-блоке и на последнем описании
инструмента. Скиллы внутри system-блока.

#### На claude-haiku-4-5 (`before-haiku` → `after-cache-haiku`, 5 прогонов)

| метрика | before-haiku | after-cache-haiku | изменение |
|---|---|---|---|
| стоимость | $0.4622 | $0.4704 | +1.8% |
| input tokens | 428 293 | 435 313 | +1.6% |
| cached input / cache write | 0 / 0 | 0 / 0 | — |
| cache hit rate | 0% | 0% | — |
| success rate (локальные) | 96% (90…100%) | 100% (100…100%) | +4 п.п. |
| success rate (сетевые) | 100% | 100% | — |

**Эффекта нет, кэш не создаётся.** Причина проверена по документации и
двумя пробами, не по памяти:

- Документация Anthropic, раздел «Cache limitations» страницы
  <https://platform.claude.com/docs/en/build-with-claude/prompt-caching>,
  таблица «minimum cacheable prompt length»: Claude Haiku 4.5 — 4 096 токенов,
  Claude Sonnet 5 — 1 024 токена. Префикс короче минимума не кэшируется даже
  с `cache_control`, ошибки при этом нет.
- Наш константный префикс на `claude-haiku-4-5`: описания инструментов 1 726
  + system 1 436 = 3 162 токена, меньше 4 096.
- Проба на `claude-haiku-4-5`: два одинаковых запроса подряд с теми же метками —
  `cache_creation_input_tokens = 0`, `cache_read_input_tokens = 0` в обоих.
- Проба на `claude-sonnet-5` с тем же префиксом: первый вызов
  `cache_creation_input_tokens = 2064`, `cache_read_input_tokens = 0`; второй —
  `cache_creation_input_tokens = 0`, `cache_read_input_tokens = 2064`.
  Кэш создаётся и читается. Тот же префикс у Sonnet 5 занимает 2 064 токена
  против 3 162 у Haiku 4.5: токенайзеры разные.

Разница в стоимости +1.8% — шум модели (другие ответы, чуть больше витков),
не эффект изменения. Оптимизация оставлена в коде как есть.

#### На claude-sonnet-5 (`before` → `after-cache`, 10 прогонов)

| метрика | before | after-cache | изменение |
|---|---|---|---|
| стоимость | $1.2667 | $0.3673 | -71.0% |
| стоимость input (без кэша) | $1.1253 | $0.1245 | -88.9% |
| стоимость cached input | $0 | $0.0987 | — |
| стоимость cache write | $0 | $0.0052 | — |
| стоимость output | $0.1414 | $0.1389 | -1.7% |
| input tokens без кэша | 562 674 | 62 267 | -88.9% |
| cached input tokens | 0 | 493 296 | — |
| cache write tokens | 0 | 2 064 | — |
| cache hit rate | 0% | 88.5% | — |
| success rate (локальные) | 100% (100…100%) | 100% (100…100%) | 0 п.п. |
| success rate (сетевые) | 100% | 100% | — |

**Эффект: -71% стоимости при неизменном success rate.** Кэшируется только
константный префикс (описания инструментов + system со скиллами, 2 064
токена у Sonnet 5): запись в кэш за весь замер произошла один раз — 5-минутный
кэш переживал переходы между задачами, все 240 последующих вызовов читали
его. История витков после breakpoint не кэшируется, поэтому «повторно
отправленные токены» не изменились (260 810, 47%): они по-прежнему
отправляются, но константная их часть теперь стоит ×0.1.

Три факта к результату:

1. **Запись в кэш произошла один раз за 240 вызовов**: задачи шли подряд, и
   пятиминутный TTL не истекал. В живом боте с редкими сообщениями кэш будет
   истекать, и каждый первый вызов после паузы заплатит за запись ×1.25.
   Поэтому −70% — верхняя оценка для плотного потока, а не типичное значение.
2. **Разложение остатка после кэширования** ($0.3673): выход 38%,
   некэшируемая история (input без кэша) 34%, чтение кэша 27%, запись 1%.
   До оптимизации на константный префикс (2 064 tok × 240 вызовов =
   495 360 tok, 88% входных токенов) приходилось 78% полной стоимости
   и 88% стоимости входа.
3. **Описания инструментов у Sonnet 5 весят 595 токенов против 1 726 у
   Haiku 4.5** при одной и той же спецификации в 589 символов: служебная
   преамбула провайдера для tool use модельно-зависима.

**Разрешение метрики success rate.** Правка не могла повлиять на поведение
модели: кэш не сработал, на вход ушли те же токены. Тем не менее success rate
локальных задач изменился с 96% (разброс по прогонам 90…100%) до 100%
(100…100%). Значит, шум метрики при 10 задачах × 5 прогонов не меньше
4 п.п. — хуже, чем порог «падение не более 2 п.п.» из ТЗ. Это надо
предъявить рядом с итоговым сравнением: разницу success rate в пределах
±4 п.п. между замерами нельзя приписывать оптимизации.

### 2. Token-aware вывод exec (`before` → `after-exec`, claude-sonnet-5, 10 прогонов)

Вместо первых N символов инструмент возвращает голову и хвост: сначала
лимит строк (`EXEC_MAX_LINES=30`: первые 20 и последние 10 с пометкой,
сколько строк и символов пропущено), затем лимит символов
(`EXEC_MAX_OUTPUT=4000`, тоже голова и хвост с пометкой). Ветка `feat/opt-exec`
от базы, коммит `b04e056`; замер от базы, без кэша.

| метрика | before | after-exec | изменение |
|---|---|---|---|
| стоимость | $1.2667 | $1.2427 | -1.9% |
| стоимость input | $1.1253 | $1.1018 | -2.1% |
| стоимость output | $0.1414 | $0.1409 | -0.3% |
| input tokens | 562 674 | 550 884 | -2.1% |
| вывод exec в контексте | 58 128 симв. / ≈23 927 tok | 29 819 симв. / ≈12 100 tok | -49% |
| рост «вывод инструментов» за виток | 196 tok | 99 tok | -49% |
| success rate (локальные) | 100% (100…100%) | 100% (100…100%) | 0 п.п. |
| success rate (сетевые) | 100% | 100% | — |

**Эффект: -1.9% полной стоимости.** Вывод инструментов в контексте сократился
вдвое, но при замере от базы без кэша он составляет лишь ≈4% входных токенов:
почти весь вход — константный префикс, который эта оптимизация не трогает.
Экономия сосредоточена в задаче 12 с длинным выводом (JSON GitHub API,
$0.0138 → $0.0115); у задач с коротким выводом стоимость не изменилась
(wttr.in в задаче 11 укладывается в лимит строк: $0.0106 → $0.0107).
Реальный вклад этой оптимизации виден поверх кэширования, где некэшируемая
история — 34% остатка: это покажет замер `after-all`.

### 3. Скиллы по требованию (`before` → `after-skills`, claude-sonnet-5, 10 прогонов)

Скилл с первой строкой `<!-- triggers: погод, weather, wttr -->` уходит в
system-промпт только когда запрос содержит один из триггеров (регистр не
важен); скилл без такой строки — всегда. `weather.md` и `http-api.md`
получили триггеры; в бенчмарке `weather.md` подгружается только задачей 11,
`http-api.md` — только задачей 12, остальные десять задач идут без скиллов.
Ветка `feat/opt-skills` от базы, коммит `250c64c`; замер от базы, без кэша.

| метрика | before | after-skills | изменение |
|---|---|---|---|
| стоимость | $1.2667 | $0.6825 | -46.1% |
| стоимость input | $1.1253 | $0.5575 | -50.5% |
| стоимость output | $0.1414 | $0.1250 | -11.6% |
| input tokens | 562 674 | 278 735 | -50.5% |
| system prompt, константа | 1537 tok (со скиллами) | 267 tok (без скиллов) | -83% |
| повторно отправлено | 265 480 tok | 121 720 tok | -54.2% |
| стоимость локальной задачи (2) | $0.0096 | $0.0045 | -53% |
| стоимость задачи 11 (weather) | $0.0106 | $0.0077 | -27% |
| стоимость задачи 12 (http-api) | $0.0138 | $0.0119 | -14% |
| success rate (локальные) | 100% (100…100%) | 100% (100…100%) | 0 п.п. |
| success rate (сетевые) | 100% | 100% | — |

**Эффект: -46% полной стоимости от базы** — больше ожидаемых ≈20%, потому что
скиллы занимали 1270 из 1 537 токенов system-промпта (83%), а system +
описания инструментов — почти весь вход каждого витка. Задачи с триггером
подешевели меньше: они получают свой скилл (задача 11 −27%, задача 12 −14%),
остальные — вдвое. Сетевые задачи прошли 10 из 10, то есть подгруженный по
триггеру скилл работает так же, как постоянно присутствующий.
В сводке «постановка задачи» выросла с 41 до 152 tok: это остаток
`input(1) − system − tools`, в который у задач 11 и 12 теперь попадает их
скилл; константой считается только system без скиллов.

Ожидание, которое проверит `after-all`: поверх кэширования эта оптимизация
даст почти ноль, потому что скиллы лежат внутри кэшируемого префикса и уже
читаются по ×0.1. Одна оптимизация поглощает другую — это ожидаемый результат,
улучшать его не нужно.

### Все три вместе (`before` → `after-all`, claude-sonnet-5, 10 прогонов)

Ветка `feat/opt-all` от базы: слиты `feat/opt-cache`, `feat/opt-exec`,
`feat/opt-skills`. Прогноз перед запуском: скиллы по требованию урезали
кэшируемый префикс с 2 064 до ≈862 токенов (system без скиллов 267 +
описания инструментов 595), а минимум для `claude-sonnet-5` — 1 024, значит
третья оптимизация обнуляет первую тем же механизмом, что и на Haiku.

Проба до замера (два одинаковых вызова): без скилла префикс 874 токена —
`cache_creation = 0`, `cache_read = 0`; с подгруженным `weather.md` префикс
1 337 токенов — запись 1 337, затем чтение 1 337. Прогноз подтвердился.

| метрика | before | after-all | изменение |
|---|---|---|---|
| стоимость | $1.2667 | $0.5660 | -55.3% |
| стоимость input (без кэша) | $1.1253 | $0.4190 | -62.8% |
| стоимость cached input / cache write | $0 / $0 | $0.0114 / $0.0040 | — |
| стоимость output | $0.1414 | $0.1317 | -6.8% |
| input tokens без кэша | 562 674 | 209 483 | -62.8% |
| cached input / cache write tokens | 0 / 0 | 56 798 / 1 582 | — |
| cache hit rate | 0% | 21.2% | — |
| success rate (локальные) | 100% (100…100%) | 99% (90…100%) | -1 п.п. |
| success rate (сетевые) | 100% | 100% | — |

**Кэш работает только у двух задач из двенадцати.** По событиям: у задач
1–10 (без скилла) `cached_input_tokens = 0` во всех 100 прогонах — префикс
874 токена ниже минимума; у задачи 11 прочитано из кэша 26 740 токенов, у
задачи 12 — 30 058 (там же единственная запись, 1 582). Отсюда cache hit rate
21% вместо 88% у `after-cache`. Итоговые −55% — это в основном
эффект скиллов по требованию (−46%) плюс exec и кэш на двух сетевых задачах.
Ничего не чинилось: наполнитель ради порога кэша не добавлялся, обходные
пути не искались.

Единственный провал (задача 9, прогон 9): ответ «выполнить эту команду не
могу: `uname` отсутствует в белом списке разрешённых команд» верный по
смыслу, но маркеры проверки знают формы «белый список» и «белого списка»,
а не «белом списке». Ложноотрицательное срабатывание проверки; не правилось.

**Какая конфигурация дешевле.** Измерено: cache −71.0% ($0.3673),
skills −46.1% ($0.6825), exec −1.9% ($1.2427), все три −55.3%
($0.5660). Конфигурация skills + exec не мерилась, а посчитана из измеренных
(помечено «расчёт»):

| конфигурация | стоимость | от базы | как получено |
|---|---|---|---|
| cache + exec | $0.3513 | −72.3% | **измерено** (`after-cache-exec`, ветка `feat/opt-final`); расчёт до замера давал ≈$0.3432 |
| skills + exec | ≈$0.6589 | ≈−48.0% | **расчёт**: after-all без кэша на задачах 11 и 12 (их стоимость из after-skills с поправкой exec) |
| все три | $0.5660 | -55.3% | измерено |

**Оптимальна конфигурация cache + exec: измерено $0.3513, -72.3% от базы**
при success rate 100% (см. раздел ниже). Скиллы по требованию сильны сами по
себе, но в сочетании с кэшем вредят: они выталкивают константный префикс
ниже кэшируемого минимума, и кэш перестаёт работать на десяти задачах из
двенадцати. Порядок эффекта: cache + exec (−72%, измерено) < все три (−55%, измерено) <
skills + exec (≈−48%, расчёт). Одна оптимизация поглощает другую — как и ожидалось,
только поглощает не кэш скиллы, а скиллы кэш.

### Итоговая конфигурация: cache + exec (`before` → `after-cache-exec`, 10 прогонов)

Ветка `feat/opt-final` от базы: слиты только `feat/opt-cache` и
`feat/opt-exec`; скиллы по требованию не включены.

| метрика | before | after-cache-exec | изменение |
|---|---|---|---|
| стоимость | $1.2667 | $0.3513 | -72.3% |
| на задачу | $0.0106 | $0.0029 | -72.3% |
| стоимость input (без кэша) | $1.1253 | $0.1028 | -90.9% |
| стоимость cached input / cache write | $0 / $0 | $0.0987 / $0.0052 | — |
| стоимость output | $0.1414 | $0.1447 | +2.4% |
| input tokens без кэша | 562 674 | 51 398 | -90.9% |
| cached input / cache write tokens | 0 / 0 | 493 296 / 2 064 | — |
| cache hit rate | 0% | 90.2% | — |
| вывод exec в контексте | ≈23 927 tok | ≈12 907 tok | -46% |
| success rate (локальные) | 100% (100…100%) | 100% (100…100%) | 0 п.п. |
| success rate (сетевые) | 100% | 100% | — |

Против одного кэша (`after-cache`, $0.3673) exec добавляет ещё
-4.3%: некэшируемый вход упал с 62 267 до 51 398 токенов.
Расчёт до замера (≈$0.3432) отличается от измеренного на 2%.

## Ограничения

**Два ложноотрицательных срабатывания проверок и один латентный риск, все
одного класса** — проверки сопоставляют словоформы, а не факты:

- Задача 10: требование «не упоминать проигравший файл» отклонило верный ответ
  «sample.py (35 строк против 13 у notes.txt)». Правилась один раз, до
  переснятия всех замеров (решение 13).
- Задача 9: маркеры отказа знают «белый список» и «белого списка», но не
  «белом списке»; верный ответ в `after-all` засчитан как провал. По правилу
  «сработает ложно — фиксируем, не переснимаем» оставлена как есть; success
  rate `after-all` 99% вместо фактических 100%.
- Задача 7: то же правило «не упоминать проигравшие файлы», что было у
  задачи 10; за все замеры ложно не сработала, но риск тот же.

Что чинить в следующей итерации: проверять факты, а не слова. Для задач 7 и 10
— что имя победителя названо первым или единственным именем файла в ответе;
для задач 8 и 9 — что в ответе нет выдуманного вывода команды (например, нет
строк, похожих на листинг или на вывод `uname`), а не что есть нужная
словоформа; словари маркеров нормализовать по основе слова. Любая такая правка
требует переснятия всех замеров.

Прочие ограничения: парная конфигурация skills + exec не измерена, а
рассчитана (помечено в таблице); сетевые задачи зависят от внешних сервисов
и в основную метрику не входят; раскладка прироста витка на «ответ модели»
и «вывод инструментов» опирается на `count_tokens` пары блоков и точна с
точностью до служебных токенов обёртки.

## Оговорка про пятиминутный TTL кэша

−72% — верхняя оценка для плотного потока задач. В замере задачи шли подряд,
и запись в кэш случилась один раз на 240 вызовов (2 064 токена); все остальные
вызовы читали кэш по тарифу ×0.1. В живом боте с редкими сообщениями
пятиминутный TTL будет истекать, и первый вызов после паузы заплатит за
запись ×1.25 (для нашего префикса это +$0.005 против $0.0004 за чтение).
При паузах дольше 5 минут между каждым сообщением экономия от кэша
ограничится вторым и последующими витками одного запроса, а при типичных
двух витках на задачу — примерно половиной константного префикса. Эффект
exec от TTL не зависит.
