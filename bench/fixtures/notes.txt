Заметки к бенчмарку агента.

Этот файл нужен только как фикстура: агент читает его инструментом exec.
Содержимое не меняется между замерами, иначе сравнение «до/после» теряет смысл.

Список фикстур:
- sample.py — небольшой модуль на Python;
- numbers.csv — колонка чисел для суммирования;
- notes.txt — этот текст.

Слово-маркер для задачи про номер строки: pelican.

Конец файла.
